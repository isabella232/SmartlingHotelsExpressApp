<!DOCTYPE html>
<html>
<head>
    <%include ../partials/head.ejs%>

    <script>
        $(document).ready(function(){
            $('#returnToSearch').remove();
            $('#selector').on("change", function(){
                var str = $('#selector').val();
                $('#findHotels').prop("href", "/browse/" + str);
            });
        });
    </script>

</head>
<body>

<%include ../partials/pageheader.ejs%>
<%var hotel = {"prevLink":""}%>
<%include ../partials/browse_form.ejs%>

<div class="container">

    <!-- Page Heading -->
    <div class="row">
        <div class="col-lg-12">
            <div class="h1 col-md-8">
                Showing hotels located in "<%= city%>"
            </div>
            <div class="h1 col-md-3 col-md-offset-1 text-right">
                <small><%= results %> of <%= results%> results shown.</small>
            </div>
        </div>
    </div>
    <!-- /.row -->

    <hr>

    <!-- Pagination -->
    <div class="row text-center">
        <div class="col-lg-12">
            <ul class="pagination">
                <li>
                    <a href="#">«</a>
                </li>
                <li class="active">
                    <a href="#">1</a>
                </li>
                <li>
                    <a href="#">»</a>
                </li>
            </ul>
        </div>
    </div>
    <!-- /.row -->

    <hr>

    <!-- Footer -->
    <% include ../partials/footer.ejs%>

</div>

</body>
</html>
